package androidx.cardview.widget;

import android.content.Context;
import android.content.res.ColorStateList;
import androidx.cardview.widget.CardView;

public interface CardViewImpl {
    void a(CardViewDelegate cardViewDelegate, ColorStateList colorStateList);

    ColorStateList b(CardViewDelegate cardViewDelegate);

    void c(CardViewDelegate cardViewDelegate);

    float d(CardViewDelegate cardViewDelegate);

    void e(CardViewDelegate cardViewDelegate, float f);

    float f(CardViewDelegate cardViewDelegate);

    void g(CardViewDelegate cardViewDelegate, float f);

    void h(CardView.a aVar, Context context, ColorStateList colorStateList, float f, float f2, float f3);

    void i(CardViewDelegate cardViewDelegate);

    float j(CardViewDelegate cardViewDelegate);

    void k(CardViewDelegate cardViewDelegate, float f);

    float l(CardViewDelegate cardViewDelegate);

    float m(CardViewDelegate cardViewDelegate);

    void n(CardViewDelegate cardViewDelegate);
}
